<?xml version="1.0" encoding="utf-8"?>
<!--
  RemoteViews-safe: no layout_weight, no autoSize (both break on many launchers).
  Content is vertically centered via FrameLayout + layout_gravity="center".
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_daily_quote_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="14dp"
        android:paddingBottom="10dp">

        <ImageView
            android:id="@+id/widget_quote_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:contentDescription="@null"
            android:importantForAccessibility="no"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_widget_format_quote" />

        <TextView
            android:id="@+id/widget_quote_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:fontFamily="serif"
            android:gravity="center"
            android:lineSpacingMultiplier="1.08"
            android:maxLines="14"
            android:textColor="@color/widget_quote_body"
            android:textSize="@dimen/widget_quote_body_size"
            android:textStyle="italic" />

        <TextView
            android:id="@+id/widget_quote_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:gravity="center_horizontal"
            android:letterSpacing="0.04"
            android:maxLines="2"
            android:textColor="@color/widget_quote_author"
            android:textSize="@dimen/widget_quote_author_size"
            android:textStyle="normal"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
